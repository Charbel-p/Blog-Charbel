# Build automatique Railway (Nixpacks) — plus simple que Docker
[phases.install]
cmds = [
    "composer install --no-dev --optimize-autoloader --no-interaction",
    "npm install",
    "npm run build",
    "chmod -R 775 storage bootstrap/cache || true"
]

[start]
cmd = "sh docker/entrypoint.sh"
